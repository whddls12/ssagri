현업에서는 크롤링을 거의 사용하지 않는다. disallow가 많고, 부정확한 데이터가 많다. 
실제로는 사업장을 통해 데이터를 구매하고 DB에 저장하고 가공하여 통계를 낸다.

셀리니움이라는 툴을 이용하여 크롤링을 사용한다.
이것을 이용하면 크롤링을 할 수 있을 것이라 생각한다.

현업에서는 크롤링보다는 스크래핑 작업을 통해 데이터를 축적한다.
신한은행과 제휴를 맺은 이지샵을 통해 회원의 데이터를 받아서 DB에 저장하고 적재하고 화면에 보여준다.

---

클라우드 기술을 금융권에서는 아직 제대로 사용하지 않는다.
금융권 서버를 클라우드로 한다고 하는데, 아직 진행 중이라 답변드리기 쉽지 않다.

---

돈에 대한 불안감을 느낄 때, 바로바로 피드백이 없다면 신뢰도가 떨어진다고 생각한다.
그래서 고객센터는 있으면 좋다. FAQ를 제대로 적어준다면, 오류조치를 디테일하게 전달할 수 있어서 고객센터의 역할을 축소할 수 있다. 하지만, 고객과 소통할 수 있는 창구는 필요하다.

---

금융권에서 중요하게 생각하는 이슈와 역량.
신한은행에서도 개발하는 범위가 넓다보니 역량 역시 본인이 지원하는 분야에 따라 상이하다.
API 사용에 대한 제한이 있거나, 이체 개발을 해 본 사람이 없어서 금융에 대한 지식을 갖추고 들어오는 사람은 거의 없다. 그러므로 없어도 무방하다. 개발적인 역량으로는 native는 모바일 OS 안드로이드와 IOS를 사용하고 그 외에는 Java와 C를 사용하고 있으니 이런 역량을 갖추면 좋겠다.

방어로직은 트랜잭션 당 통신 횟수에 제한을 걸어서 방어를 한다. 거래를 분할처리하는 방식으로 처리한다.

---

가상결제 플랫폼 API를 만들어보려고 하는데, 현실성이 있는지 여쭤봅니다.

==> 좋습니다. 돈이 결제만 되는 것이 아니라, 돈이 지급되는 과정을 배울 수 있습니다. 중간 PG사를 통해서 가는 것은 맞습니다. 어디에 취업하시든 결제 시스템은 만들어볼 수 있을 것입니다. 블랙박스에 대한 고민을 할 수 있는 기회입니다. 공부도 하고 알아가는 부분에서는 좋은 기회라고 생각합니다. 가상으로 결제되는 API를 만든다면 메리트 있는 좋은 경험이 될 것입니다. 거기에 돈이 왔다갔다 할 때 검증하는 로직에 더 고민해보면 좋겠다. 큰 의의가 있다고 생각한다.

은행 서버에서 클라이언트에서 외부망을 통할 때, 보안과정이 있고, 데이터를 직렬화하는 과정을 거친다고 들었습니다. 데이터는 전부 중요하지만, 상대적으로 중요한 데이터와 덜 중요한 데이터가 있을텐데, 이러한 데이터를 모두 데이터를 가공하는 과정을 거칠까요?

==> 사업자번호는 개인정보가 아니라 보안과정을 거치지 않아도 된다. 그러나, 개인정보는 클라이언트를 거치지 않고 서버로 바로 전송된다. 이 데이터를 받을 때 내부적으로 OPENAPI를 거치기 전에 데이터를 한 번 점검 후 API를 호출합니다. 인계되는 정보와 출력되는 정보에 따라 암호화나 데이터가 흐르는 로직이 다르긴합니다.

---

DB가 중요하다!!

인프라, 터미널, DevOPs, CLI, 운영

Java 개발도 할 줄 알면서, 인프라적인 지식까지 알고있다면 큰 메리트가 될 수 있을 것이다.

운영을 많이 할 줄 알았는데, 개발:운영 비율은 5:5 ~ 8:2 정도이다. 그러나 팀마다 상황이 많이 다르다.